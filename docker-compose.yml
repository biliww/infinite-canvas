services:
  app:
    image: ghcr.io/biliww/infinite-canvas:latest
    container_name: infinite-canvas
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "13000:3000"
    restart: unless-stopped
